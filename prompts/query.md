# 🤖 prompts/query.md — 위키에 질문하기

위키에 쌓인 지식으로 질문에 답합니다. **원본을 매번 재검색하지 않고**,
이미 컴파일된 요약·집계 페이지를 읽어 답합니다 (CLAUDE.md §5.3, No-RAG §8).

## 쓰임새 예
- "이번 달 가장 약한 단원은?" · "부호 실수가 줄고 있어?"
- "이차방정식에서 내가 자주 틀리는 패턴 정리해줘"
- 주간 리뷰 생성 · 시험 전 약점 브리핑

---

## 📋 프롬프트 (복사해서 사용)

```text
너는 이 저장소의 위키 관리자다. CLAUDE.md를 먼저 읽는다.
아래 질문에 답하되, 먼저 wiki/index.md로 탐색한 뒤 필요한 페이지만 읽는다.
(임베딩 RAG 쓰지 말 것. 규모가 작으니 위키를 직접 읽어 답한다.)

[질문]
(여기에 질문)

[규칙]
- 근거가 된 위키 페이지를 함께 인용(링크).
- 데이터가 없으면 "아직 데이터 없음"이라고 솔직히 말한다(추측 금지).
- 중3 학생에게 하듯 쉽고 따뜻하게, 단 정확하게.
```

---

## 🗓️ 주간 리뷰 모드 (특수 query)

```text
위 규칙대로, 이번 주(YYYY-MM-DD ~ YYYY-MM-DD)의 wiki/problems · weakness-map을 읽고
wiki/dashboard/weekly-review.md 형식으로 주간 회고를 작성해 그 파일 맨 위에 추가해줘.
형식: 숫자 / 잘한 점 / 반복 약점 / 다음 주 목표(3개 이하) / 한마디.
작성 후 wiki/log.md에 "query | weekly-review | <주차>" 한 줄 기록.
```

---

## 💡 팁
- query는 **읽기 위주**라 위키를 바꾸지 않습니다(주간 리뷰 모드는 예외 — 리뷰 1건 추가).
- 자주 묻는 질문은 대시보드 페이지로 굳혀두면 다음엔 바로 보입니다.
